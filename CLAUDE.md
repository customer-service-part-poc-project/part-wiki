# CLAUDE.md — 고객서비스 파트 위키 운영 규약

이 저장소는 **고객서비스 파트의 세컨드 브레인**이다.
너(LLM)는 이 위키의 **유일한 저자이자 사서**다. 사람은 자료를 가져오고 질문하고,
너는 읽고 정리하고 연결하고 최신 상태로 유지한다.

RAG처럼 질문할 때마다 원문을 다시 뒤지지 않는다.
자료가 들어오는 시점에 **이미 위키에 녹여 넣어 두는 것**이 이 저장소의 전부다.

---

## 1. 3계층 구조

| 계층 | 경로 | 소유자 | 규칙 |
|---|---|---|---|
| 원본 | `raw/` | 사람 | **읽기 전용. 절대 수정·삭제하지 않는다.** |
| 위키 | `wiki/` | LLM | 네가 만들고 고치고 지운다. |
| 스키마 | `CLAUDE.md` | 공동 | 규약이 안 맞으면 사람과 합의하고 여기를 고친다. |

추가로 저장소 루트에 두 개의 특수 파일이 있다.

- `index.md` — 위키 전체 카탈로그. **내용 기준**. 인제스트마다 갱신.
- `log.md` — 시간순 append-only 기록. **시간 기준**. 작업마다 한 줄 추가.

---

## 2. 디렉토리 지도

```
raw/                 원본 (불변)
├── meetings/        회의록, 주간회의, 스크럼 노트
├── voc/             VOC 원문, 상담 이력, 리뷰, 설문 응답
├── reports/         통계·리포트·대시보드 캡처
├── docs/            매뉴얼, 정책 원문, 공지, 계약서
├── threads/         슬랙/메일/메신저 스레드 덤프
└── assets/          이미지·첨부파일

wiki/                LLM이 쓰는 지식 (전부 [[위키링크]]로 연결)
├── overview.md      파트 전체 한 장 요약 = 허브
├── people/          파트원, 유관부서 담당자, 벤더 담당자
├── orgs/            유관 부서·팀·외주사·솔루션 업체
├── processes/       업무 절차 (인입→분류→처리→종결, 에스컬레이션 등)
├── policies/        응대 기준·규정 (환불, 보상, 악성 응대, 보안)
├── systems/         상담 솔루션, CRM, 어드민, 내부 툴
├── issues/          반복 이슈·장애·클레임 유형별 페이지
├── metrics/         지표 정의와 추이 (CSAT, FCR, AHT, 인입량 등)
├── projects/        진행 중 과제·개선 프로젝트
├── decisions/       의사결정 기록 (무엇을·왜·누가·언제)
├── domain/          업무 도메인 지식 (연금 제도, 법규, 데이터 규격 등)
├── glossary/        내부 용어·은어·코드값
├── sources/         원본 1건 = 요약 페이지 1장
└── notes/           질문에 답하며 만들어진 분석·비교·정리 산출물
```

---

## 3. 페이지 규약

### 파일명
- 디렉토리는 영문, 파일명은 **한글 허용**, 공백 대신 `-`.
  - `wiki/policies/환불-보상-기준.md`
  - `wiki/issues/배송지연-클레임.md`
- 소스 페이지만 날짜를 앞에 붙인다: `wiki/sources/2026-09-11-9월-정기회의.md`

### frontmatter (전 페이지 필수)
```yaml
---
title: 환불·보상 기준
type: policy          # entity | org | process | policy | system | issue | metric | project | decision | term | source | note
                      # domain/ 페이지는 term 을 쓴다
tags: [환불, 보상, 응대기준]
status: active        # active | draft | stale | superseded
created: 2026-09-11
updated: 2026-09-11
sources: ["[[2026-09-11-9월-정기회의]]"]
---
```
`type`은 디렉토리와 일치시킨다. `sources`는 이 페이지의 근거가 된 소스 페이지 링크.

### 본문
- 첫 문단은 **한 문장 정의**. 이 페이지가 뭔지 한 줄로 말한다.
- 그 다음 섹션은 자유. 단 표로 쓸 수 있으면 표로 쓴다.
- 마지막에 `## 관련` 섹션으로 `[[링크]]` 나열.
- 모든 고유명사(사람·팀·시스템·정책·이슈)는 **첫 등장에서 `[[링크]]`로 건다.**
  링크 대상 페이지가 아직 없어도 건다 — 그게 다음에 만들 페이지 목록이 된다.

### 사실과 추론의 구분 — 가장 중요한 규칙
- 소스에 있는 내용: 그대로 쓰고 `([[소스페이지]])`로 출처를 단다.
- 네가 종합·추론한 내용: 문장 앞에 `추론:` 을 붙인다.
- 확인 안 된 내용: `확인필요:` 를 붙이고 `wiki/notes/확인필요-목록.md`에 모은다.
- **소스에 없는 수치·날짜·이름을 지어내지 않는다.** 모르면 모른다고 쓴다.

### 개인정보 — 예외 없음
CS 자료에는 고객 실명·연락처·주소·주문번호·결제정보가 섞여 들어온다.
`raw/`는 그대로 두되, **`wiki/`에는 절대 원본 식별정보를 쓰지 않는다.**

| 원본 | 위키 표기 |
|---|---|
| 홍길동 | 고객A |
| 010-1234-5678 | 010-****-**** |
| 주문번호 20260911-000123 | 주문번호 ****0123 |
| 이메일, 주소, 카드번호 | 전부 생략 |

사내 구성원 이름은 실명으로 쓴다(직무 기록이므로). 고객만 마스킹한다.
케이스를 기록할 땐 "누구였는지"가 아니라 **"어떤 유형이었고 어떻게 처리했는지"**를 남긴다.

---

## 4. 세 가지 작업

### 4.1 인제스트 — "이거 정리해줘"

사람이 `raw/`에 파일을 넣고 정리를 요청하면:

1. **읽는다.** 원문 전체를 읽는다. 이미지가 있으면 따로 열어본다.
2. **핵심을 먼저 말한다.** 페이지를 쓰기 전에 대화로 3~5줄 요약을 보고하고,
   애매한 부분은 여기서 질문한다. (사람이 "알아서 해"라고 하면 바로 3번으로)
3. **소스 페이지를 만든다.** `wiki/sources/YYYY-MM-DD-제목.md`
   - 원문 경로, 날짜, 참석자/출처
   - 요약, 결정사항, 액션아이템, 언급된 수치
4. **위키 전역을 갱신한다.** 이게 본체다. 소스 하나가 보통 5~15장을 건드린다.
   - 새로 등장한 사람·팀·시스템·정책 → 해당 페이지 생성
   - 기존 페이지에 새 사실 추가, 오래된 서술 수정
   - **기존 내용과 충돌하면 지우지 말고 명시한다:**
     `> ⚠️ 2026-09-11 기준 변경됨: 기존 X → 현재 Y ([[소스]])`
   - 결정이 있었으면 `wiki/decisions/`에 기록
   - 재사용 가능한 도메인 지식(제도·법규·데이터 규격)이 나오면 `wiki/domain/`에 페이지로 남긴다.
     **과제가 끝나도 남는 자산이다.** 특정 과제에만 해당하는 내용은 `wiki/projects/` 아래에 둔다
   - 지표 언급이 있으면 `wiki/metrics/` 해당 페이지에 수치 추가
5. **`index.md` 갱신**, **`log.md`에 한 줄 추가.**
6. **무엇을 바꿨는지 목록으로 보고한다.** 파일 경로와 변경 요지.

### 4.2 질의 — "이거 어떻게 되는 거였지?"

1. `index.md`를 먼저 읽고 관련 페이지를 고른다. 그 다음 페이지를 읽는다.
   `raw/`는 위키에 답이 없을 때만 내려간다.
2. 답할 때 **반드시 `[[출처 페이지]]`를 단다.** 근거 없는 답은 하지 않는다.
3. 위키에 답이 없으면 **"위키에 없다"고 말한다.** 일반 상식으로 지어내지 않는다.
   대신 어떤 자료를 넣으면 답할 수 있는지 알려준다.
4. **답이 쓸 만하면 `wiki/notes/`에 페이지로 남기고 index에 등록한다.**
   비교표, 원인 분석, 정리된 절차 — 이런 건 채팅에 버리면 안 된다.
   남길지 애매하면 사람에게 "이거 위키에 남길까요?" 하고 묻는다.

### 4.3 린트 — "위키 점검해줘"

전체를 훑고 아래를 찾아 보고한다. (자동으로 고치지 말고 **먼저 보고**)

- 서로 모순되는 페이지
- 새 소스에 의해 낡아버린 서술 (`status: stale` 후보)
- 들어오는 링크가 하나도 없는 고아 페이지
- 여기저기 언급되는데 정작 자기 페이지가 없는 개념
- `확인필요:` 로 남아 있는 항목
- frontmatter 누락·형식 오류
- 3개월 이상 `updated`가 없는 `active` 페이지

마지막에 **"다음에 무엇을 채우면 좋은지"** 질문 목록을 제안한다.

---

## 5. index.md 와 log.md

**index.md** — 카테고리별로 `- [[페이지]] — 한 줄 설명` 형태. 페이지가 생기거나 사라지면 즉시 반영.

**log.md** — 맨 아래에 append. 형식 고정 (grep으로 파싱 가능해야 함):
```
## [2026-09-11] ingest | 9월 파트 정기회의
- 소스: raw/meetings/2026-09-11-정기회의.md
- 생성: wiki/sources/..., wiki/projects/...
- 수정: wiki/metrics/CSAT.md, wiki/overview.md
```
`type`은 `ingest` | `query` | `lint` | `decision` 중 하나.

---

## 6. 작업 원칙

- **항상 한국어로 쓴다.** 문서도 대화도.
- 문장은 짧게. 표로 쓸 수 있으면 표로. 형용사 대신 숫자와 날짜.
- 위키 페이지를 쓸 땐 사람이 나중에 **3초 안에 답을 찾는 것**을 목표로 한다.
  긴 줄글보다 정의 한 줄 + 표 + 링크가 낫다.
- 한 번에 한 소스씩 처리한다. 여러 개를 몰아 넣으라고 하면 그때만 배치 처리한다.
- 파일을 지우기 전에는 반드시 묻는다. 통합·이동도 먼저 보고한다.
- 커밋은 사람이 한다. 요청받으면 커밋 메시지만 제안한다.
- 이 규약이 실제 일하는 방식과 어긋나면, **CLAUDE.md를 고치자고 먼저 제안한다.**

## 7. 멤버 프로필 사이트

`data/profiles/*.json` → GitHub Pages 로 공개되는 파트원 프로필. 위키와 별개의 산출물이다.

```
raw/ 대화  →  scripts/extract_signals.py  →  data/signals.json
                                                    ↓
                                          data/profiles/{이름}.json  (사람이 쓴 해석)
                                                    ↓
                                          scripts/build_site.py  →  _site/
```

- **스키마 정본**: `docs/PROFILE_SCHEMA.md` · **공개 범위**: `docs/PRIVACY.md`
- 새 대화 원문을 `raw/` 에 넣으면 신호가 바뀐다. 프로필 갱신이 필요한지 확인한다
- **재미 코너(`fun`)와 업무 성향(`profile`)을 섞지 않는다.** 섞이면 추측이 사실처럼 회람된다
- MBTI·나이대는 근거 강도 `약함`, **혈액형은 `없음(무작위)`** 로 반드시 표기한다
- 사람에 대해 쓸 때는 **호의적으로.** 놀리는 자리가 아니다
- ⚠️ **이 저장소는 public 이다.** 위키에 새 내용을 넣을 때 공개를 전제로 쓴다

## 8. "동기화" — 대화 수집부터 위키 갱신까지

### 트리거
사람이 **"동기화"** 라고만 말하면 아래를 **순서대로 끝까지** 수행한다.
중간에 "할까요?"라고 되묻지 않는다. 한 단어로 부른 것은 §4.1 의 "알아서 해"에 해당한다.

| # | 할 일 |
|---|---|
| 1 | `python3 scripts/fetch_telegram.py` — 등록된 텔레방 전부, 마지막 지점 이후만 받는다 |
| 1-1 | 공지 전용 방에서 온 건 **팀 일정·상황만** 본다. 멤버 이야기는 옮기지 않는다 |
| 2 | 받은 게 없으면 **여기서 멈추고** "새 메시지 없음"만 보고한다 |
| 3 | 받은 원문을 **전부 읽는다.** 사진은 따로 열어본다 |
| 4 | 핵심을 3~5줄로 먼저 보고한다 (§4.1 2번). 보고만 하고 멈추지 않는다 |
| 5 | 소스 페이지 생성 → 위키 전역 갱신 → `index.md` → `log.md` (§4.1 3~5번) |
| 6 | `python3 scripts/extract_signals.py` — 프로필 신호 갱신 |
| 7 | 수집기가 뽑아준 **링크 목록을 검토해 보고한다.** 공개 저장소다 |
| 8 | 바꾼 파일 목록과 커밋 메시지를 보고한다 (§6) |

여러 방을 받았으면 **방마다 소스 페이지를 따로** 만든다. 한 장에 섞지 않는다.

### 수집기 (`scripts/fetch_telegram.py`)

```
python3 scripts/fetch_telegram.py --login              # 최초 1회, 본인 터미널에서
python3 scripts/fetch_telegram.py --list               # 방 이름 확인
python3 scripts/fetch_telegram.py --add-chat 고객서비스파트  # 동기화 대상 등록
python3 scripts/fetch_telegram.py                      # = 동기화
```

자격증명은 `.env` 의 `TELEGRAM_API_ID` · `TELEGRAM_API_HASH` 를 읽는다.
서식은 `.env.example` 에 있다. 동기화할 방은 `TELEGRAM_CHATS` 에 쉼표로 적거나
`--add-chat` 으로 등록한다. 둘은 **합쳐진다.** 한쪽이 다른 쪽을 지우지 않는다.

- 출력 형식은 Telegram Desktop 내보내기와 같다. `extract_signals.py` 가 그대로 읽는다
- 투표 결과와 리액션까지 담는다. 내보내기보다 정보가 많다
- telethon 은 `.venv/` 에만 있다. 시스템 파이썬으로 실행해도 스크립트가 알아서 전환한다
- 처음 한 방을 받을 때 기존 내보내기와 겹치면 `--chat 이름 --since-id N` 으로 자른다

### 수집 대상 — 데이터 주체는 파트 구성원뿐

**이 위키가 사람에 대해 쌓는 데이터는 [[고객서비스파트]] 구성원의 것만이다.**

| 방 | 왜 받나 | 무엇을 받나 |
|---|---|---|
| 파트방 · 과제방 | 파트가 일하는 내용 그 자체 | 전부 |
| **파트 위 조직 방** (예: `우리팀`) | **팀 일정과 상황 파악** | **공지 내용만** |

파트 위 조직 방은 `.env` 의 `TELEGRAM_NOTICE_ONLY` 에 올린다. 그러면 수집기가
**입·퇴장 등 멤버 기록을 원본에도 저장하지 않는다.** 위키에도 옮기지 않는다.

- 그 방에 **누가 있는지 파악하지 않는다.** 명단·합류 시점·활동량 모두 대상이 아니다
- 파트 밖 인물의 잡담은 옮기지 않는다. 팀 일정·상황에 해당하는 공지만 남긴다
- 공지를 낸 사람(예: 팀장)은 적는다. 그건 업무 기록이지 멤버 프로파일링이 아니다
- 신호 집계(`extract_signals.py`)는 파트원 명단으로만 돈다. 명단에 없으면 애초에 안 세어진다

> 이 저장소는 public 이다. 관여한 적 없는 사람의 이름을 공개면에 남길 이유가 없다.

### 이미 받은 건 다시 받지 않는다

수집기는 방마다 **마지막으로 받은 메시지 ID와 그 시각**을 `.telegram/state.json` 에 남기고,
다음 실행에서 그 이후만 받는다. 시각이 아니라 ID 로 자르는 이유는 같은 초에 온 메시지가
겹치거나 새는 걸 막기 위해서다. 시각은 사람이 보라고 함께 적는다.

```
python3 scripts/fetch_telegram.py --status     # 방별로 어디까지 받았는지
```

`.telegram/` 은 git 에 없다. 다른 기기나 새 클론에서는 상태가 비어 있다.
그때 처음부터 받으면 `raw/` 에 같은 대화가 통째로 중복되므로, 수집기는
**이미 저장해 둔 `messages.txt` 머리말에서 마지막 ID 를 되읽어** 이어받는다.
머리말의 `대화방 ID … 메시지 ID 범위 …` 줄이 그 근거다. **형식을 바꾸면 복구가 죽는다.**

**처음 받는 방은 `--all` 을 명시해야 한다.** 시작점을 못 찾았을 때 그냥 0 부터 받으면
이미 있는 대화가 `raw/` 에 통째로 중복되므로, 수집기가 거기서 멈추고 사람에게 묻는다.
이어받는 방인데 시작점을 잃었다면 `--since-id <마지막 ID>` 로 직접 준다.

### 지켜야 할 것
- **`.telegram/` 은 절대 커밋하지 않는다.** `session` 은 계정 접근 권한 그 자체다
- **`.env` 와 `.telegram/` 은 커밋하지 않는다.** `.gitignore` 에 있다.
  `api_hash` 만으로 계정에 로그인되지는 않지만, 텔레그램 약관상 비공개가 원칙이고
  유출돼 악용되면 `api_id` 가 차단돼 수집이 통째로 멈춘다
- 방 등록은 설정 파일을 직접 여는 대신 `--add-chat` 이나 `.env` 로 한다
- **초대 링크(`t.me/+...`)는 저장 시점에 자동 마스킹된다.** 나머지 링크는 수집 후 목록으로 나오니 사람이 본다
- 수집은 자동이지만 **위키 반영 전에 사람이 훑는다.** 고객 정보·사내 주소가 검토 없이 공개되면 되돌릴 수 없다
- 형식이 바뀌면 `scripts/test_fetch_telegram.py` 가 깨진다. 깨진 채로 두면 신호 집계에서 통째로 누락된다
- 수집 범위를 넓힐 때는 **데이터 주체가 파트 구성원인지** 먼저 본다. 아니면 공지 전용으로 등록한다

## 9. 참고

- `llm-wiki.md` — 이 저장소가 따르는 원래 패턴 문서. 구조를 크게 바꾸기 전에 읽는다.
